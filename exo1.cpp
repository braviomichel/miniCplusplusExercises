#include "Cercle.h"
#include "Cercle_col.h"
#include <iostream>
using namespace std;

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
bool compare(Cercle c1,Cercle c2)
{
	return((c1.x==c2.x)&&(c1.y==c2.y)&&(c1.r==c2.r)&&(c1.nom==c2.nom));
}

int main(int argc, char** argv) {
	
	return 0;
}
